# Veto Backend
Hello! This is the backend for the Veto app.

We're using rust with the axum framework to handle http requests and postgresql to store data.
To get this working you will need to first setup [rust](https://rust-lang.org/) and [postgres](https://www.postgresql.org/).

To run:
`cargo run`

